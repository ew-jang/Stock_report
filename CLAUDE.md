# CLAUDE.md

이 저장소는 **종목 분석 멀티 에이전트 시스템**입니다. 사용자가 종목을 제시하면
여러 전문 에이전트가 협업하여 최종 투자 리포트를 작성합니다.

## 종목 분석 워크플로우 (필수)

사용자가 특정 종목에 대한 분석/투자 판단을 요청하면 (예: "삼성전자 분석해줘",
"엔비디아 어때?", "카카오 투자해도 돼?"), 아래 절차를 **반드시** 따른다.

### 1단계 — 3개 에이전트 병렬 실행

다음 세 에이전트를 **단일 메시지에서 동시에(병렬)** 실행한다. 순차 실행하지 않는다.

- `financial-analyst` — 재무 분석 (최근 3년 매출/영업이익률/순이익률, PER·PBR·ROE,
  부채비율·유동비율, 재무 건전성 등급)
- `news-sentiment-analyst` — 최근 1개월 뉴스 감성 분석 (호재/악재 분류 및 시장 심리 판정)
- `sector-outlook-analyst` — 업종 분석 (글로벌 시장 흐름, 경쟁사 실적, 정책·규제, 업종 전망)

세 에이전트에는 동일한 종목명/티커를 전달한다.

### 2단계 — 종합 리포트 작성

세 에이전트의 결과가 **모두 반환된 뒤에만**, `investment-synthesizer` 에이전트
(공격적·수익 극대화 관점의 투자 전략가)를 실행한다. 이때 세 분석 결과 전문을
입력으로 전달하여 다음을 포함한 최종 리포트를 받는다.

- 진입 타이밍, 목표 수익률, 손절 라인
- 세 관점을 종합한 공격적 투자 판단

### 3단계 — 리포트 파일 저장 (필수)

종합 리포트를 사용자에게 보여주는 것과 별개로, **항상** `report/` 폴더에
마크다운 파일로 저장한다.

- 경로: `report/{종목명}_{YYYY-MM-DD}.md` (예: `report/삼성전자_2026-06-28.md`)
- 날짜는 분석 수행일(오늘 날짜)을 사용한다.
- 저장할 내용: 세 분석가의 핵심 요약 + 각 분석 상세 + 전략가의 최종 투자 판단
  (진입 타이밍·목표 수익률·손절 라인 포함). 즉 화면에 제시한 종합 리포트 전문.
- `report/` 폴더가 없으면 생성한다(Write 도구로 파일을 만들면 자동 생성됨).
- 같은 종목을 같은 날 재분석하면 해당 파일을 덮어쓴다.

### 출력

- 모든 리포트는 **한국어**로 작성한다.
- 최종 응답에는 (1) 세 분석가의 핵심 요약과 (2) 전략가의 종합 판단을 함께 제시한다.
- 응답 말미에 저장된 리포트 파일 경로를 알려준다.

## 규칙

- 1단계의 세 에이전트는 서로 의존성이 없으므로 항상 병렬로 호출한다.
- 2단계는 1단계 세 결과에 의존하므로, 셋이 모두 끝난 뒤 실행한다.
- 3단계 파일 저장은 종목 분석을 수행할 때마다 **예외 없이** 실행한다.
- 단순 정보 조회(예: "오늘 삼성전자 주가")는 이 워크플로우를 적용하지 않아도 된다.
